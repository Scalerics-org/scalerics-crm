[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn server:app --bind 0.0.0.0:$PORT --workers 1 --threads 4"
healthcheckPath = "/login"
healthcheckTimeout = 10
restartPolicyType = "on_failure"
